# Dotenv file (contains sensitive API keys)
.env

# Node.js modules (can be reinstalled with npm install)
node_modules/

# Other common ignore patterns for Node.js
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store